steps:
  # 🏗 Step 1: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-t',
        'us-central1-docker.pkg.dev/$PROJECT_ID/my-app-repo/myapp:latest',
        '.'
      ]

  # 📦 Step 2: Push image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      ['push', 'us-central1-docker.pkg.dev/$PROJECT_ID/my-app-repo/myapp:latest']

  # 🚀 Step 3: Get GKE credentials
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      ['container', 'clusters', 'get-credentials', 'my-cluster', '--zone', 'us-central1-a']

  # 🌱 Step 4: Apply Deployment and Service YAMLs
  - name: 'gcr.io/cloud-builders/kubectl'
    args: ['apply', '-f', 'deployment.yaml']

  - name: 'gcr.io/cloud-builders/kubectl'
    args: ['apply', '-f', 'service.yaml']

images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/my-app-repo/myapp:latest'

options:
  logging: CLOUD_LOGGING_ONLY
